rootProject.name = 'lwjgl'

sourceControl {
    gitRepository(new URI("https://github.com/dzikalogika/suite.git")) {
        producesModule("lpo:suite")
    }

    gitRepository(new URI("https://github.com/dzikalogika/jorg.git")) {
        producesModule("lpo:jorg")
    }
}